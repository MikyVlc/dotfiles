#!/bin/sh

[ -f "$HOME/.dmenurc" ] && . "$HOME/.dmenurc"

exec dmenu_run \
    -p " Run" \
    -i \
    -fn "$DMENU_FN" \
    -nb "$DMENU_NB" \
    -nf "$DMENU_NF" \
    -sb "$DMENU_SB" \
    -sf "$DMENU_SF"
